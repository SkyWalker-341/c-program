#include <stdio.h>
#include <math.h>
int main()
{
 int num1,num2,sum,product,quotient;
 printf("enter the num1 value ");
 scanf("%d",&num1);
 printf("enter the num2 value ");
 scanf("%d",&num2);
 sum = num1+num2;
 printf("the sum value is %d\n",sum);
 product = num1*num2;
 printf("the product volume is %d\n",product);
 quotient = num1%num2;
 printf("the quotient value is %d\ns",quotient);
 return 0;
 }
